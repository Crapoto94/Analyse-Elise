services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: odata-stats-app
    restart: always
    ports:
      - "5004:5004"
    environment:
      - DATABASE_URL_SYSTEM=file:/app/prisma/system.db
      - ODATA_BASE_URL=https://ville-ivry94.illico.city/AppBI/odata/
      - ODATA_USERNAME=User_StatBI
      - ODATA_PASSWORD=2V.}dyRB,8P9h6]8=Fte
      - ADMIN_EMAIL=admin@elise.local
      - ADMIN_PASSWORD_HASH=8c6976e5b5410415bde908bd4dee15dfb167a9c873fc4bb8a81f6f2ab448a918
      - SESSION_SECURE=false
      - AUTH_SECRET=ivry-sur-seine-secret-key-2024
      - NODE_ENV=production
      - PORT=5004
    command: >
      sh -c "npx prisma@6.19.2 db push --accept-data-loss --skip-generate && node server.js"
    volumes:
      - ./prisma:/app/prisma
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5004/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  default:
    name: odata-network
